[Unit]
Description=Timer for materialize feature rows into redis

[Timer]
OnCalendar=hourly
# A laptop that was asleep catches up once rather than never.
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
